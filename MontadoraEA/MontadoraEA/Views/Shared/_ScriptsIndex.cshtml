<script>
        $(function () {
            $(".cadastrar").click(function () {
                $("#divModal").load("@HttpContext.Current.Request.RequestContext.RouteData.Values["controller"].ToString()/Novo", function () {
                    $("#divModal").modal();
                })
            });
        })

        $(function () {
            $(".visualizar").click(function () {
                var id = $(this).attr("data-id");
                $("#divModal").load("@HttpContext.Current.Request.RequestContext.RouteData.Values["controller"].ToString()/Visualizar?id=" + id, function () {
                    $("#divModal").modal();
                })
            });
        })

        $(function () {
            $(".editar").click(function () {
                var id = $(this).attr("data-id");
                $("#divModal").load("@HttpContext.Current.Request.RequestContext.RouteData.Values["controller"].ToString()/Editar?id=" + id, function () {
                    $("#divModal").modal();
                })
            });
        })

        $(function () {
            $(".excluir").click(function () {
                var id = $(this).attr("data-id");
                $("#divModal").load("@HttpContext.Current.Request.RequestContext.RouteData.Values["controller"].ToString()/Excluir?id=" + id, function () {
                    $("#divModal").modal();
                })
            });
        })

        $(document).ready(function () {
            $('#tabelaId').dataTable({
                "language": {
                    "sEmptyTable": "Nenhum registro encontrado",
                    "sInfo": "Mostrando de _START_ até _END_ de _TOTAL_ registros",
                    "sInfoEmpty": "Mostrando 0 até 0 de 0 registros",
                    "sInfoFiltered": "(Filtrados de _MAX_ registros)",
                    "sInfoPostFix": "",
                    "sInfoThousands": ".",
                    "sLengthMenu": "_MENU_ resultados por página",
                    "sLoadingRecords": "Carregando...",
                    "sProcessing": "Processando...",
                    "sZeroRecords": "Nenhum registro encontrado",
                    "sSearch": "Pesquisar",
                    "oPaginate": {
                        "sNext": "Próximo",
                        "sPrevious": "Anterior",
                        "sFirst": "Primeiro",
                        "sLast": "Último"
                    },
                    "oAria": {
                        "sSortAscending": ": Ordenar colunas de forma ascendente",
                        "sSortDescending": ": Ordenar colunas de forma descendente"
                    }
                }
            });
        });
</script>

